<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <data noupdate="1">
        <record id="ir_cron_check_retraite_alert" model="ir.cron">
            <field name="name">Check Retirement Alerts</field>
            <field name="model_id" ref="aft_hr.model_hr_employee"/>
            <field name="state">code</field>
            <field name="code">model.check_retraite_alert()</field>
            <field name="interval_number">1</field>
            <field name="interval_type">days</field>
            <field name="numbercall">-1</field> <!-- -1 signifie sans limite -->
            <!-- Pas besoin de spécifier nextcall ici -->
        </record>
    </data>
</odoo>